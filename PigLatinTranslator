pyg = 'ay'

original = raw_input('Enter a word:')

word = original.lower()
first = word[0]
restofword = word[1:9]
new_word = restofword + first + pyg





if len(original) > 0 and original.isalpha():
    print new_word
else:
    print 'empty'
